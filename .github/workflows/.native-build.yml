name: Native Build

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  compile:
    name: Clean Package (Native)
    runs-on: ubuntu-latest
    container:
      image: maven:3.9.6-eclipse-temurin-21
    steps:
      - uses: actions/checkout@v4

      - name: Run Maven clean package
        run: PROFILE_STATE=dev mvn clean package -Pnative -Dmaven.test.skip=true

      - name: List target
        run: ls -la target/

      - name: Upload target artifact
        uses: actions/upload-artifact@v4
        with:
          name: target
          path: target/

  build:
    name: Build Native Image
    runs-on: ubuntu-latest
    needs: compile
    container:
      image: ghcr.io/graalvm/native-image-community:21
      options: --entrypoint ""
    steps:
      - uses: actions/checkout@v4

      - name: Install Maven
        run: |
          curl -fsSL https://archive.apache.org/dist/maven/maven-3/3.9.6/binaries/apache-maven-3.9.6-bin.tar.gz | tar -xz -C /opt
          ln -s /opt/apache-maven-3.9.6/bin/mvn /usr/local/bin/mvn

      - name: Download target artifact
        uses: actions/download-artifact@v4
        with:
          name: target
          path: target/

      - name: Run native compile
        run: PROFILE_STATE=dev mvn -Pnative native:compile -Dmaven.test.skip=true

      - name: List target
        run: ls -la target/

      - name: Upload native image artifact
        uses: actions/upload-artifact@v4
        with:
          name: native-image
          path: target/
